# Issues #129, #130, #138 - English KDoc cleanup

## Context

Several Kotlin files still had Korean KDoc after recent behavior fixes. Public API and contributor-facing documentation in this repository should be English.

## Decision

Translate the targeted class KDoc, public API KDoc, and the explicitly requested inline comments in `DeclaredExposedR2dbcQuery` to English without changing runtime code.

## Outcome

The targeted KDoc now explains PartTree query execution, declared R2DBC query constraints, and Caffeine repository extension points in English.

## Verification

Checked all targeted files for Korean text inside `/** ... */` KDoc blocks; no Korean KDoc remains. Checked `DeclaredExposedR2dbcQuery.kt` for Korean text; no hits remain.

## Future Guidance

When behavior fixes touch public Kotlin APIs, scan nearby KDoc before closing the issue so language cleanup is not deferred.
