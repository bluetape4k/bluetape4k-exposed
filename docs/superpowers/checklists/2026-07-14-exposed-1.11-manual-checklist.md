# Exposed 1.11 매뉴얼 제공 체크리스트

범위: `0b494a5fd1e083006046764757342b68a397e4c5`에서 릴리스된
`bluetape4k-exposed` `1.11.0`의 40개 Gradle 프로젝트를 위한
영문·한국어 병기 소스 매뉴얼.

## 소스 및 인벤토리

- [x] **MAN-01 — 릴리스 출처 고정**
  - **작업:** 매뉴얼을 태그 `1.11.0` 및 정확한 릴리스 커밋에 연결한다.
  - **근거:** 매니페스트 릴리스 메타데이터와 릴리스 검증기가
    `0b494a5fd1e083006046764757342b68a397e4c5`를 사용한다.
  - **실패 조건:** 태그 또는 커밋이 다르면 퍼블리싱을 차단한다.
- [x] **MAN-02 — 릴리스된 모든 프로젝트 포함**
  - **작업:** 릴리스 인벤토리를 내보내고 매니페스트와 비교한다.
  - **근거:** 프로젝트 ID 40개이며, 매뉴얼 검증기가 `Manuals are aligned`를 보고한다.
  - **실패 조건:** 누락된 랜딩을 추가하거나 릴리스 대상이 아닌 프로젝트를 제거한다.
- [x] **MAN-03 — 로케일 동등성 유지**
  - **작업:** 영문과 한국어 랜딩, 가이드, 장 구성을 일치시킨다.
  - **근거:** 로케일별 Markdown 파일 62개, 총 124개이다.
  - **실패 조건:** 로케일이 누락되거나 의미가 변경되면 퍼블리싱을 차단한다.

## 콘텐츠 및 소스 링크

- [x] **MAN-04 — 스캐폴드를 소스 소유 매뉴얼로 교체**
  - **작업:** 릴리스 소스를 기준으로 소유권, API 작업, 구성, 실패 진단,
    운영, 제한 사항, 학습 경로를 설명한다.
  - **근거:** 40개의 영문·한국어 병기 랜딩과 상세 JDBC/R2DBC 장, 프레임워크,
    데이터, 캐시, 코덱, 방언, 예제, 벤치마크 가이드를 제공한다.
  - **실패 조건:** 일반적인 README 재진술이나 근거 없는 동작은 거부한다.
- [x] **MAN-05 — 릴리스 링크 검증**
  - **작업:** 저장소 기준 상대 소스 링크를 정확한 릴리스 트리에 대해 모두 확인한다.
  - **근거:** `1.11.0`에 대해 `456 checked, 0 missing`이다.
  - **실패 조건:** 디렉터리, develop 전용 또는 누락된 링크를 정확한 릴리스 파일로
    교체하거나 해당 주장을 제거한다.
- [x] **MAN-06 — 소비자 버전 중앙화 유지**
  - **작업:** `bluetape4k-dependencies` 플랫폼을 표시하고 개별 bluetape4k
    라이브러리 버전은 생략한다.
  - **근거:** 매뉴얼과 루트 README의 의존성 예제가 중앙 BOM을 사용한다.
  - **실패 조건:** 중복된 프로젝트/BOM/라이브러리 버전 선언을 제거한다.

## 다이어그램 품질

- [x] **DIA-01 — 모든 SVG/PNG 쌍 등록**
  - **작업:** 표준 소스와 렌더링된 에셋을 매니페스트에 유지한다.
  - **근거:** 10개의 SVG/PNG 쌍에서 생성된 파일 20개이다.
  - **실패 조건:** 소스, PNG, embed 또는 매니페스트 항목이 누락되면 퍼블리싱을 차단한다.
- [x] **DIA-02 — 커넥터 파싱, 렌더링 및 감사**
  - **작업:** 새 프레임워크/배치 다이어그램마다 XML, CairoSVG `-s 2`, 커넥터,
    기하, 엔드포인트 및 혼합 코너 검사를 실행한다.
  - **근거:** JDBC `6/9`, R2DBC `6/9`, Modulith `7/9`, Batch `12/13`
    커넥터/카드 수를 확인했으며, 침범, 교차, 기하, 엔드포인트 및 혼합 코너
    실패는 모두 0이다. Repository Overview `9/9` 및 Adapter Map `10/11`;
    침범, 교차, 기하, 엔드포인트 및 혼합 코너 실패는 모두 0이다.
  - **실패 조건:** 에셋 하나를 수정하고 해당 에셋의 전체 루프를 반복한다.
- [x] **DIA-03 — 기준 PNG 검사**
  - **작업:** 마지막 렌더링 후 모든 최종 PNG를 원본 크기로 연다.
  - **근거:** 어두운 테마, 읽기 쉬운 텍스트, 표시되는 14x14 기본 화살촉,
    연결된 경로 및 잘림 없음; 오래된 R2DBC 제목 렌더링이 감지되어 완료 전에
    재생성되었다. 이제 Repository Overview는 JDBC 파란색과 R2DBC 청록색을
    구분하며, Adapter Map은 대비되는 대상 색상을 사용한다. 두 최종 PNG 모두
    재렌더링 후 원본 크기로 다시 검사했다.
  - **실패 조건:** PNG가 스크립트 결과를 무효화하면 SVG 수정으로 돌아간다.
## 재현 가능한 검증

- [x] **VER-01 — 결정적으로 내보내기**
  - **작업:** 생성된 매니페스트를 두 번 내보낸다.
  - **증거:** 두 SHA-256 값은
    `6751c4f510375a878b333634415de9682b6fc78cdd978af0eaae2f3ab07c31a1`이다.
  - **실패:** 커밋하기 전에 비결정적 순서 또는 콘텐츠를 수정한다.
- [x] **VER-02 — 문서 계약 실행**
  - **작업:** 다섯 개의 Ruby 계약 스위트를 모두 실행한다.
  - **증거:** 27회 실행, 323개 어설션, 실패 0건, 오류 0건.
  - **실패:** 실패한 계약을 수정하고 종속 검증기를 다시 실행한다.
- [x] **VER-03 — 대상 실행 가능 예제 실행**
  - **작업:** 매뉴얼에서 구체적인 런타임 주장을 하는 모듈을 검증한다.
  - **증거:** Batch 355개 통과/7개 보류; Ktor 라이브러리 8개 통과; Spring
    Boot Batch 65개 통과; Spring Modulith 61개 통과; Timefold 48개 통과;
    Ktor 데모 1개 통과; JDBC 데모 26개 통과; R2DBC 데모 25개 통과.
  - **실패:** 제공 전에 매뉴얼 또는 구현 증거를 수정한다.
- [x] **VER-04 — 문서 CI 비례성 유지**
  - **작업:** 프로덕션 코드와 무관한 작업에 전체 Kotlin 매트릭스를 사용하지 않는다.
  - **증거:** 로컬 인벤토리, 계약, 릴리스 링크 검증, 대상 모듈 테스트 및 다이어그램 게이트가 변경된 동작 주장을 보장한다.
  - **실패:** 로컬 재현성만으로 퍼블리싱 입력을 보호할 수 없을 때에만 저비용 문서 전용 워크플로를 추가한다.
- [x] **VER-05 — diff 위생 점검**
  - **작업:** `git diff --check`를 실행하고 예상된 파일만 포함되었는지 확인한다.
  - **증거:** 공백 오류가 없고 프로덕션 코드 변경이 없다.
  - **실패:** 관련 없는 변경 또는 잘못된 변경을 커밋 전에 제거한다.

## GitHub 전달

- [x] **GH-01 — 이슈 메타데이터 생성 및 검증**
  - **작업:** 소스 이슈를 생성하고, `debop`을 할당하며, 의도한 레이블과 함께 마일스톤
    `1.11.0`을 설정한다.
  - **증거:** 이슈 `#376`이 `debop`에 할당되고 `documentation`,
    `ci`, `github_actions` 레이블 및 `1.11.0` 마일스톤이 설정되었으며, 라이브
    `gh issue view` 출력으로 확인된다.
  - **실패:** PR을 생성하거나 병합하기 전에 메타데이터를 수정한다.
- [x] **GH-02 — PR 메타데이터 생성 및 검증**
  - **작업:** `develop`을 대상으로 PR을 생성하고, 이슈 메타데이터를 동일하게 적용하며, `debop`을 할당하고, `## DoD Status`를 마지막 레벨 2 제목으로 유지한다.
  - **증거:** PR `#377`이 `develop`을 대상으로 하고 `debop`에 할당되었으며, 이슈의
    `documentation`, `ci`, `github_actions` 레이블과 `1.11.0`
    마일스톤을 동일하게 적용하고 `## DoD Status`로 끝나며, 라이브 `gh pr view` 출력으로 확인된다.
  - **실패:** 검토 또는 병합 전에 라이브 PR을 수정한다.
- [ ] **GH-03 — 검사 및 병합 검증**
  - **작업:** 병합 직전에 라이브 검사를 읽고, 필수 검사 중 실패한 것이 없을 때만 병합하며, 병합 커밋을 기록한다.
  - **증거:** `gh pr checks`, 병합된 PR 상태 및 병합 SHA.
  - **실패:** PR을 열어 둔 채 실패한 게이트를 수정한다.
- [ ] **GH-04 — 로컬 develop 동기화**
  - **작업:** 브랜치가 통합되었음이 조상 관계로 입증된 후에만 실제 체크아웃을 fast-forward하고 워크트리를 제거한다.
  - **증거:** 로컬/upstream `develop` SHA가 일치하고 clean 상태가 유지된다.
  - **실패:** 파괴적인 정리 없이 전달을 보류 상태로 남긴다.

## 현재 합계

- 필수 검사: 16/18
- N/A: 0
- 차단됨: 0
- 보류 중: `GH-03`, `GH-04`
