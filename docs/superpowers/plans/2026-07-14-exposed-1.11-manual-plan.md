# Exposed 1.11 소스 매뉴얼 구현 계획

> **에이전트 작업자용:** 필수 하위 스킬: 이 계획을 작업별로 구현할 때 superpowers:subagent-driven-development(권장) 또는 superpowers:executing-plans를 사용한다. 진행 상태는 체크박스(`- [ ]`) 문법으로 추적한다.

**목표:** 소스 저장소에 릴리스 내용을 충실히 반영한 `bluetape4k-exposed` 1.11 이중 언어 매뉴얼을 구축한다. 릴리스 `1.11.0`의 Gradle 프로젝트 40개를 모두 다루고 JDBC, R2DBC, 캐시, 데이터베이스 어댑터, 통합, 예제, 운영을 학습할 수 있는 매뉴얼 수준의 경로를 제공한다.

**아키텍처:** 소스 저장소가 `docs/manual/manifest.yaml`, 이중 언어 Markdown, 어두운 테마의 기술 다이어그램, Ruby 검증기를 소유한다. Gradle은 현재 모듈 인벤토리를 내보내고, 릴리스 검증기는 저장소 상대 경로로 된 모든 소스 참조를 커밋 `0b494a5fd1e083006046764757342b68a397e4c5`의 태그 `1.11.0`에 고정한다. 웹사이트는 두 계약을 모두 통과한 뒤 생성된 정규화 매니페스트만 사용한다.

**기술 스택:** Kotlin/Gradle, Ruby 표준 라이브러리와 Minitest, YAML/JSON, Markdown, SVG/PNG, Git 릴리스 트리 검사

---

## 파일 구성

`/Users/debop/work/bluetape4k/bluetape4k-exposed`에서 다음 구현 단위를 생성하거나 수정한다.

- `build.gradle.kts`: 결정적 Gradle 프로젝트 인벤토리를 `build/manual/module-inventory.json`으로 내보낸다.
- `scripts/manual/manual_contract.rb`: 스키마, 인벤토리 일치 여부, 이중 언어 파일, 필수 섹션, 저장소 경로, 장, SVG/PNG 자산 쌍을 검증한다.
- `scripts/manual/validate_manuals.rb`: 작성 트리 계약의 명령줄 진입점이다.
- `scripts/manual/manual_contract_test.rb`: 계약 수준의 실패 및 성공 테스트다.
- `scripts/manual/release_contract.rb`: 안정 태그 SHA와 저장소 상대 경로로 된 모든 매뉴얼 링크를 릴리스 트리와 대조해 검증한다.
- `scripts/manual/validate_release_manuals.rb`: 안정 릴리스 검증의 명령줄 진입점이다.
- `scripts/manual/release_contract_test.rb`: 태그, SHA, 안전하지 않은 링크, 누락 링크, 성공 테스트다.
- `scripts/manual/export_manifest.rb`: YAML을 사이트에서 사용할 수 있는 결정적 JSON으로 정규화한다.
- `scripts/manual/export_manifest_test.rb`: 결정적 내보내기 및 스키마 테스트다.
- `scripts/manual/release_inventory.rb`: 프로젝트 42개인 develop 인벤토리에서 릴리스 `1.11.0`에 빌드 파일이 존재하는 정확히 40개 프로젝트만 필터링한다.
- `scripts/manual/release_inventory_test.rb`: 이동된 태그, 중복, 빈 결과, 예상 개수, 42개에서 40개로의 필터링 테스트다.
- `scripts/manual/generate_manuals.rb`: 작성된 페이지를 덮어쓰지 않고 누락된 이중 언어 모듈 스캐폴드를 생성한다.
- `scripts/manual/generate_manuals_test.rb`: 보존, 로케일 frontmatter, 소스 링크 깊이 테스트다.
- `docs/manual/manifest.yaml`: 프로젝트 40개의 기준 인벤토리, 장, 자산, 저장소 상대 소스/테스트/예제 경로를 담는다. 저장소 간 학습 링크는 일반 본문 링크로 유지한다.
- `docs/manual/generated/manifest.json`: 사이트가 수집하도록 커밋하는, 생성된 정규화 매니페스트다.
- `docs/manual/templates/module.md`, `docs/manual/templates/example.md`, `docs/manual/templates/benchmark.md`: 유형별 스캐폴딩 계약이다.
- `docs/manual/en/index.md`, `docs/manual/ko/index.md`: 제품 개요, 버전 출처, 모듈 맵, 학습 경로 진입점이다.
- `docs/manual/en/getting-started.md`, `docs/manual/ko/getting-started.md`: 중앙 BOM 설정과 첫 JDBC/R2DBC 선택을 설명한다.
- `docs/manual/en/architecture/repository-map.md`, `docs/manual/ko/architecture/repository-map.md`: Build/Learn/Apply 저장소 맵이다.
- `docs/manual/{en,ko}/guides/*.md`: 영속성 선택, 트랜잭션 소유권, 캐시 선택, 데이터베이스 어댑터, 테스트, 운영, 학습 경로를 다룬다.
- `docs/manual/{en,ko}/modules/*.md`: 안정 Gradle 프로젝트마다 랜딩 페이지 하나를 제공한다.
- `docs/manual/{en,ko}/modules/<priority-module>/*.md`: 아래에 나열한 주요 경로의 상세 장이다.
- `docs/manual/assets/<topic>/*.svg`와 `.png`: 어두운 테마의 기술 다이어그램 쌍이다.
- `docs/superpowers/checklists/2026-07-14-exposed-1.11-manual-checklist.md`: 모듈 작업군, 로케일, 릴리스 링크, 다이어그램 검토 증거다.
- `README.md`, `README.ko.md`: 소스 매뉴얼로 연결하는 간결한 진입 링크다.
- `AGENTS.md`: 매뉴얼 소유권, 안정 릴리스 제외 항목, 집중 검증 명령을 정의한다.

안정 프로젝트 40개의 인벤토리는 다음 ID로 고정한다.

```text
bluetape4k-exposed-bigquery
bluetape4k-exposed-bom
bluetape4k-exposed-cache
bluetape4k-exposed-clickhouse
bluetape4k-exposed-cockroachdb
bluetape4k-exposed-core
bluetape4k-exposed-dao
bluetape4k-exposed-duckdb
bluetape4k-exposed-fastjson2
bluetape4k-exposed-jackson2
bluetape4k-exposed-jackson3
bluetape4k-exposed-jdbc-caffeine
bluetape4k-exposed-jdbc-lettuce
bluetape4k-exposed-jdbc-redisson
bluetape4k-exposed-jdbc-tests
bluetape4k-exposed-jdbc
bluetape4k-exposed-measured
bluetape4k-exposed-mysql8
bluetape4k-exposed-postgresql
bluetape4k-exposed-r2dbc-caffeine
bluetape4k-exposed-r2dbc-lettuce
bluetape4k-exposed-r2dbc-redisson
bluetape4k-exposed-r2dbc-tests
bluetape4k-exposed-r2dbc
bluetape4k-exposed-starrocks
bluetape4k-exposed-timefold-solver-persistence
bluetape4k-exposed-tink
bluetape4k-exposed-trino
bluetape4k-exposed-batch
bluetape4k-exposed-ktor
bluetape4k-exposed-spring-boot-jdbc
bluetape4k-exposed-spring-boot-r2dbc
bluetape4k-exposed-spring-boot-batch
bluetape4k-exposed-spring-modulith
examples-exposed-bigquery-dry-run
examples-exposed-clickhouse-oltp-olap
examples-ktor-exposed-demo
exposed-spring-boot-jdbc-demo
exposed-spring-boot-r2dbc-demo
benchmark-exposed-benchmark
```

`bluetape4k-exposed-druid`와 `examples-ddd-spring-modulith-demo`는 릴리스 `1.11.0`에 존재하지 않으므로 제외한다.

### 작업 1: 격리된 소스 worktree를 만들고 안정 기준선을 고정한다

**파일:**
- 생성: `docs/superpowers/specs/2026-07-14-exposed-1.11-manual-design.md`
- 생성: `docs/superpowers/plans/2026-07-14-exposed-1.11-manual-plan.md`

- [x] **1단계: 현재 `origin/develop`에서 격리된 worktree를 생성한다**

```bash
git fetch origin --prune
git worktree add .worktrees/docs-exposed-1.11-manual -b docs/exposed-1.11-manual origin/develop
```

예상 결과: 기본 체크아웃을 변경하지 않고 `docs/exposed-1.11-manual`의 깨끗한 worktree가 생성된다.

- [x] **2단계: 작성을 시작하기 전에 릴리스 기준을 검증한다**

```bash
git rev-parse 1.11.0^{commit}
git ls-tree -r --name-only 1.11.0 > /tmp/bluetape4k-exposed-1.11.0-tree.txt
```

첫 번째 명령의 예상 결과: `0b494a5fd1e083006046764757342b68a397e4c5`.

- [x] **3단계: 승인된 설계와 이 실행 계획을 소스 저장소에 복사한다**

소스 설계에는 다음 내용을 정확히 기록해야 한다.

```yaml
repository: bluetape4k/bluetape4k-exposed
releaseRef: 1.11.0
releaseCommit: 0b494a5fd1e083006046764757342b68a397e4c5
minorVersion: "1.11"
moduleCount: 40
excludedDevelopOnlyModules:
  - bluetape4k-exposed-druid
  - examples-ddd-spring-modulith-demo
```

- [x] **4단계: 소스가 소유하는 설계와 계획을 커밋한다**

```bash
git add docs/superpowers/specs/2026-07-14-exposed-1.11-manual-design.md docs/superpowers/plans/2026-07-14-exposed-1.11-manual-plan.md
git commit -m "Define a stable source contract for the Exposed manual" \
  -m "Constraint: The 1.11 manual must describe tag 1.11.0 even though develop already contains 1.12-only modules." \
  -m "Confidence: high" \
  -m "Scope-risk: narrow" \
  -m "Tested: release tag and commit identity verified." \
  -m "Not-tested: Manual implementation has not started."
```

### 작업 2: Gradle 모듈 인벤토리를 내보낸다

**파일:**
- 수정: `build.gradle.kts`
- 테스트: `build/manual/module-inventory.json`

- [ ] **1단계: 실패하는 인벤토리 단언을 추가한다**

구현 전에 실행한다.

```bash
./gradlew exportManualModuleInventory
```

예상 결과: `Task 'exportManualModuleInventory' not found` 메시지와 함께 FAIL한다.

- [ ] **2단계: 루트 게시 설정 근처에 결정적 내보내기 작업을 등록한다**

다음 작업 본문을 그대로 추가한다. 저장소는 게시 자격을 판단할 때 `isNonPublishedModule()`을 사용하지만, 이 문서 인벤토리에는 해당 필터를 적용하면 안 된다. 게시하지 않는 예제와 벤치마크를 포함해 등록된 모든 하위 프로젝트를 포함해야 한다.

```kotlin
val manualModuleInventory = layout.buildDirectory.file("manual/module-inventory.json")

tasks.register("exportManualModuleInventory") {
    group = "documentation"
    description = "Exports the registered Gradle project inventory for manual validation."

    val repositoryRoot = project.rootDir.toPath()
    val modules = project.subprojects.sortedBy(Project::getPath).map { module ->
        val sourceDir = repositoryRoot.relativize(module.projectDir.toPath())
            .toString().replace(File.separatorChar, '/')
        val kind = when {
            sourceDir.startsWith("examples/") -> "example"
            sourceDir.startsWith("benchmark/") -> "benchmark"
            else -> "library"
        }
        linkedMapOf(
            "gradlePath" to module.path,
            "projectName" to module.name,
            "sourceDir" to sourceDir,
            "kind" to kind,
        )
    }
    val inventoryJson = groovy.json.JsonOutput.prettyPrint(groovy.json.JsonOutput.toJson(modules)) + "\n"
    inputs.property("inventoryJson", inventoryJson)
    outputs.file(manualModuleInventory)
    doLast {
        manualModuleInventory.get().asFile.apply {
            parentFile.mkdirs()
            writeText(inventoryJson)
        }
    }
}
```

- [ ] **3단계: 안정 작성 인벤토리 형태를 실행하고 단언한다**

```bash
./gradlew exportManualModuleInventory
jq 'length' build/manual/module-inventory.json
jq -r '.[].projectName' build/manual/module-inventory.json | sort -u | wc -l
```

현재 develop의 예상 결과: `42`와 `42`. 이후 매니페스트 검증기는 작성한 안정 매니페스트를 릴리스 인벤토리와 의도적으로 비교해야 하며, develop에만 있는 두 프로젝트를 암묵적으로 허용하면 안 된다.

- [ ] **4단계: 인벤토리 작업을 커밋한다**

```bash
git add build.gradle.kts
git commit -m "Expose the module boundary needed for manual validation" \
  -m "Constraint: Documentation coverage must include examples and benchmarks, not only published artifacts." \
  -m "Confidence: high" \
  -m "Scope-risk: narrow" \
  -m "Tested: ./gradlew exportManualModuleInventory; jq inventory uniqueness checks."
```

### 작업 3: 작성 및 릴리스 검증기를 테스트 우선으로 추가한다

**파일:**
- 생성: `scripts/manual/manual_contract.rb`
- 생성: `scripts/manual/manual_contract_test.rb`
- 생성: `scripts/manual/validate_manuals.rb`
- 생성: `scripts/manual/release_contract.rb`
- 생성: `scripts/manual/release_contract_test.rb`
- 생성: `scripts/manual/validate_release_manuals.rb`
- 생성: `scripts/manual/export_manifest.rb`
- 생성: `scripts/manual/export_manifest_test.rb`
- 생성: `scripts/manual/release_inventory.rb`
- 생성: `scripts/manual/release_inventory_test.rb`
- 생성: `scripts/manual/generate_manuals.rb`
- 생성: `scripts/manual/generate_manuals_test.rb`

- [ ] **1단계: 작성 계약에 대해 실패하는 Minitest 사례를 작성한다**

픽스처 매니페스트는 다음 최소 항목을 실행해야 한다.

```yaml
schemaVersion: 2
repository: bluetape4k/bluetape4k-exposed
releaseRef: 1.11.0
releaseCommit: 0b494a5fd1e083006046764757342b68a397e4c5
modules:
  - id: bluetape4k-exposed-core
    gradlePath: ":bluetape4k-exposed-core"
    sourceDir: exposed/core
    kind: library
    group: foundation
    artifact: io.github.bluetape4k.exposed:bluetape4k-exposed-core
    en: en/modules/bluetape4k-exposed-core.md
    ko: ko/modules/bluetape4k-exposed-core.md
    sourcePaths: [exposed/core/src/main/kotlin]
    testPaths: [exposed/core/src/test/kotlin]
    workshops: [examples/jdbc-demo]
```

테스트는 중복 ID, 누락된 로케일 문서, 누락된 필수 섹션, 매니페스트/인벤토리 불일치, 안전하지 않은 `../` 경로, 고립된 자산, 누락된 SVG/PNG 쌍, 잘못된 릴리스 SHA, 태그 `1.11.0`에 없는 릴리스 상대 링크의 정확한 오류를 단언해야 한다.

- [ ] **2단계: 테스트를 실행하고 계약 클래스가 없어서 실패하는지 검증한다**

```bash
ruby -Itest scripts/manual/manual_contract_test.rb
ruby -Itest scripts/manual/release_contract_test.rb
ruby -Itest scripts/manual/export_manifest_test.rb
ruby -Itest scripts/manual/release_inventory_test.rb
ruby -Itest scripts/manual/generate_manuals_test.rb
```

예상 결과: `LoadError` 또는 누락된 `ManualDocs` 상수로 인해 FAIL한다.

- [ ] **3단계: Projects 매뉴얼 스키마와 일치하는 최소 계약을 구현한다**

공개 인터페이스는 다음과 같아야 한다.

```ruby
module ManualDocs
  SUPPORTED_SCHEMA_VERSION = 2
  REQUIRED_SECTIONS = %w[
    problem when-to-use coordinates concepts quick-start api-by-task
    patterns integrations configuration failures operations testing
    workshops limitations sources
  ].freeze
  VALID_KINDS = %w[library example benchmark].freeze

  class Validator
    attr_reader :errors
    def initialize(inventory:, manifest_path:, repository_root:, expected_release:)
      @inventory = inventory
      @manifest_path = File.expand_path(manifest_path)
      @repository_root = File.expand_path(repository_root)
      @expected_release = expected_release
      @errors = validate.sort
    end
  end

  class ReleaseContract
    ValidationResult = Struct.new(:errors, :checked_count, keyword_init: true)
    def initialize(repository_root:, tag:, expected_sha:, git_runner: nil); end
    def validate; end
    def errors = validate.errors
  end
end
```

검증기는 `release_inventory.rb`가 내보낸 릴리스 필터링 인벤토리와 비교해야 한다. 따라서 예상 집합은 현재 develop의 42개가 아니라 40개다.

`release_inventory.rb`는 `tag`, `expected_sha`, 현재 인벤토리 경로, 출력 경로, 예상 개수를 받는 함수와 CLI를 제공해야 한다. 릴리스 트리에 `<sourceDir>/build.gradle.kts`가 존재할 때만 행을 유지하고, 중복 Gradle 경로, 빈 결과, 이동된 태그, 40개가 아닌 결과를 거부한다. 테스트 픽스처는 Druid와 DDD 데모가 제거되지만 `:examples-ktor-exposed-demo`는 유지됨을 입증해야 한다.

- [ ] **4단계: 결정적 매니페스트 내보내기를 구현한다**

`export_manifest.rb`는 키를 재귀적으로 정렬하고 마지막에 LF를 붙인 다음 최상위 형태를 출력해야 한다.

```json
{
  "schemaVersion": 2,
  "repository": "bluetape4k/bluetape4k-exposed",
  "releaseRef": "1.11.0",
  "releaseCommit": "0b494a5fd1e083006046764757342b68a397e4c5",
  "modules": []
}
```

- [ ] **5단계: 모든 계약 테스트를 실행한다**

```bash
ruby -Itest scripts/manual/manual_contract_test.rb
ruby -Itest scripts/manual/release_contract_test.rb
ruby -Itest scripts/manual/export_manifest_test.rb
ruby -Itest scripts/manual/release_inventory_test.rb
ruby -Itest scripts/manual/generate_manuals_test.rb
```

예상 결과: 모든 테스트가 실패 0건, 오류 0건으로 PASS한다.

- [ ] **6단계: 검증기 인프라를 커밋한다**

```bash
git add scripts/manual build.gradle.kts
git commit -m "Prevent Exposed manuals from drifting beyond their release" \
  -m "Constraint: Develop contains modules and prose that are not part of 1.11.0." \
  -m "Rejected: Validate only the current checkout | It would allow 1.12-only paths into the 1.11 manual." \
  -m "Confidence: high" \
  -m "Scope-risk: moderate" \
  -m "Tested: all manual contract and release contract Minitest suites."
```

### 작업 4: 안정 프로젝트 40개를 모두 기준 매니페스트에 등록한다

**파일:**
- 생성: `docs/manual/manifest.yaml`
- 생성: `docs/manual/generated/manifest.json`
- 생성: `docs/manual/templates/module.md`
- 생성: `docs/manual/templates/example.md`
- 생성: `docs/manual/templates/benchmark.md`
- 수정: `scripts/manual/generate_manuals_test.rb`

- [ ] **1단계: 책임별로 묶은 매니페스트 항목 40개를 작성한다**

다음 그룹을 그대로 사용한다.

```yaml
foundation: [bluetape4k-exposed-bom, bluetape4k-exposed-core, bluetape4k-exposed-dao, bluetape4k-exposed-cache]
jdbc: [bluetape4k-exposed-jdbc, bluetape4k-exposed-jdbc-tests, bluetape4k-exposed-jdbc-caffeine, bluetape4k-exposed-jdbc-lettuce, bluetape4k-exposed-jdbc-redisson]
r2dbc: [bluetape4k-exposed-r2dbc, bluetape4k-exposed-r2dbc-tests, bluetape4k-exposed-r2dbc-caffeine, bluetape4k-exposed-r2dbc-lettuce, bluetape4k-exposed-r2dbc-redisson]
serialization: [bluetape4k-exposed-fastjson2, bluetape4k-exposed-jackson2, bluetape4k-exposed-jackson3, bluetape4k-exposed-tink, bluetape4k-exposed-measured]
database: [bluetape4k-exposed-postgresql, bluetape4k-exposed-mysql8, bluetape4k-exposed-duckdb, bluetape4k-exposed-clickhouse, bluetape4k-exposed-trino, bluetape4k-exposed-bigquery, bluetape4k-exposed-starrocks, bluetape4k-exposed-cockroachdb]
integration: [bluetape4k-exposed-batch, bluetape4k-exposed-ktor, bluetape4k-exposed-spring-boot-jdbc, bluetape4k-exposed-spring-boot-r2dbc, bluetape4k-exposed-spring-boot-batch, bluetape4k-exposed-spring-modulith, bluetape4k-exposed-timefold-solver-persistence]
example: [examples-exposed-bigquery-dry-run, examples-exposed-clickhouse-oltp-olap, examples-ktor-exposed-demo, exposed-spring-boot-jdbc-demo, exposed-spring-boot-r2dbc-demo]
benchmark: [benchmark-exposed-benchmark]
```

이 분류는 안정 프로젝트 40개 전체를 포괄한다. 라이브러리 아티팩트 34개(BOM 포함), 실행 가능한 예제 5개, 벤치마크 1개다.

각 항목은 실제 Gradle 경로, 태그 상대 `sourceDir`, 라이브러리의 실제 아티팩트 좌표, 예제/벤치마크의 `null` 아티팩트, 이중 언어 문서 경로, 태그 `1.11.0`에 존재하는 소스/테스트 경로를 사용해야 한다.

- [ ] **2단계: 매니페스트가 안정 인벤토리와 정확히 일치함을 입증한다**

```bash
ruby scripts/manual/release_inventory.rb \
  1.11.0 0b494a5fd1e083006046764757342b68a397e4c5 \
  build/manual/module-inventory.json build/manual/module-inventory-1.11.0.json 40
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
```

이 단계의 예상 결과: 아직 생성하지 않은 매뉴얼 문서만 실패하며, 누락되거나 예상하지 못한 Gradle 프로젝트 오류는 없다.

- [ ] **3단계: 스캐폴드 생성 전에 유형별 템플릿 세 개를 작성한다**

다음 필수 frontmatter 계약으로 `module.md`, `example.md`, `benchmark.md`를 생성한다.

```yaml
---
id: "{{id}}"
title: "{{title}}"
locale: "{{locale}}"
kind: "{{kind}}"
gradlePath: "{{gradlePath}}"
sourceDir: "{{sourceDir}}"
releaseRef: "1.11.0"
artifact: {{artifact}}
---
```

모든 템플릿은 다음 앵커 15개를 정확히 한 번씩 렌더링해야 한다.

```markdown
## Problem {#problem}
## When to use it {#when-to-use}
## Coordinates {#coordinates}
## Core concepts {#concepts}
## Quick start {#quick-start}
## API by task {#api-by-task}
## Recommended patterns {#patterns}
## Integrations {#integrations}
## Configuration {#configuration}
## Failure modes {#failures}
## Operations {#operations}
## Testing {#testing}
## Workshops and learning path {#workshops}
## Limitations {#limitations}
## Sources {#sources}
```

`module.md`에는 null이 아닌 Maven 좌표와 API 중심 빠른 시작이 필요하다. `example.md`는 `artifact: null`을 렌더링하며 사전 요구 사항, 실행 가능한 Gradle 명령, 예상 관찰 결과, 실패 진단이 필요하다. `benchmark.md`는 `artifact: null`을 렌더링하며 워크로드, 환경, 메트릭 방향, 대표 결과, 주의 사항, 벤치마크가 입증하지 않는 내용을 명시해야 한다.

`kind`별 템플릿 선택, 두 로케일의 완전한 frontmatter 치환, 앵커 15개의 정확히 한 번씩 등장, 기존 작성 페이지의 바이트 단위 보존, 두 번째 생성 실행의 쓰기 0회를 입증하는 생성기 테스트를 추가한다.

생성기는 기존의 모든 로케일 랜딩을 작성된 콘텐츠로 취급해야 한다. 쓰기 모드로 열지 않고 건너뛰며, 생성/건너뜀 개수를 보고하고, 기존 파일을 잘라내거나 정규화해서는 안 된다. 따라서 두 번째 실행은 최종 렌더링 결과만 우연히 같아지는 것이 아니라 계약상 no-op이다.

- [ ] **4단계: 작성된 파일을 덮어쓰지 않고 누락된 이중 언어 스캐폴드를 생성한다**

```bash
ruby scripts/manual/generate_manuals.rb docs/manual/manifest.yaml
ruby scripts/manual/generate_manuals.rb docs/manual/manifest.yaml
```

예상 결과: 첫 번째 실행은 모듈 랜딩 파일 80개를 생성한다. 두 번째 실행은 생성 파일 0개를 보고하며 모든 바이트를 그대로 유지한다.

- [ ] **5단계: 정규화 JSON을 생성하고 검증한다**

```bash
ruby scripts/manual/export_manifest.rb docs/manual/manifest.yaml docs/manual/generated/manifest.json
ruby scripts/manual/export_manifest_test.rb
git diff --check
```

예상 결과: 모듈 40개를 포함하고 공백 오류가 없는 결정적 JSON이다.

- [ ] **6단계: 인벤토리, 템플릿, 생성된 랜딩을 커밋한다**

```bash
git add docs/manual/manifest.yaml docs/manual/generated/manifest.json docs/manual/templates docs/manual/en/modules docs/manual/ko/modules scripts/manual/generate_manuals_test.rb
git commit -m "Fix the Exposed 1.11 manual inventory to its stable release" \
  -m "Constraint: Druid and the DDD demo belong to the next minor and are intentionally excluded." \
  -m "Confidence: high" \
  -m "Scope-risk: narrow" \
  -m "Tested: release inventory comparison and deterministic manifest export."
```

### 작업 5: 개요, 설치, 아키텍처, 학습 경로를 작성한다

**파일:**
- 생성: `docs/manual/en/index.md`
- 생성: `docs/manual/ko/index.md`
- 생성: `docs/manual/en/getting-started.md`
- 생성: `docs/manual/ko/getting-started.md`
- 생성: `docs/manual/en/architecture/repository-map.md`
- 생성: `docs/manual/ko/architecture/repository-map.md`
- 생성: `docs/manual/en/guides/learning-path.md`
- 생성: `docs/manual/ko/guides/learning-path.md`
- 생성: `docs/manual/assets/overview/{repository-overview,module-map}.{svg,png}`
- 수정: `docs/manual/manifest.yaml`
- 수정: `docs/manual/generated/manifest.json`

- [ ] **1단계: 릴리스 출처를 포함한 이중 언어 개요 문서를 작성한다**

두 인덱스는 `1.11.0`을 소비자가 선택한 의존성 버전으로 제시하지 않으면서 다음 사실을 명시해야 한다.

```text
Consumer version authority: io.github.bluetape4k:bluetape4k-dependencies:<version>
Manual technical baseline: bluetape4k-exposed 1.11.0
Release commit: 0b494a5fd1e083006046764757342b68a397e4c5
Stable Gradle projects: 40
Primary paths: JDBC, R2DBC, cache, database adapters, application integrations
```

- [ ] **2단계: 두 시작하기 페이지에 정확한 Gradle 설정을 제공한다**

```kotlin
dependencies {
    implementation(platform("io.github.bluetape4k:bluetape4k-dependencies:<version>"))
    implementation("io.github.bluetape4k.exposed:bluetape4k-exposed-jdbc")
    // Choose this instead for coroutine-native reactive database access:
    // implementation("io.github.bluetape4k.exposed:bluetape4k-exposed-r2dbc")
}
```

애플리케이션이 습관적으로 두 경로를 모두 가져오면 안 된다고 설명한다. 드라이버 모델, 트랜잭션 소유권, 블로킹 경계, 프레임워크 통합을 기준으로 선택해야 한다.

- [ ] **3단계: 단계적으로 심화되는 순서로 학습 경로를 작성한다**

한국어 경로는 다음 내용을 자연스럽게 설명해야 한다.

1. `core`와 `dao`는 엔티티, ID, 매핑, 저장소 개념의 기반을 마련한다.
2. 블로킹 드라이버와 트랜잭션 관리자가 경계를 소유할 때는 JDBC가 프로덕션 기본값이다.
3. 드라이버, 프레임워크, 호출 체인이 계속 논블로킹으로 동작할 때 R2DBC를 선택한다.
4. 저장소와 트랜잭션 의미를 이해한 뒤에만 캐시 모듈을 선택한다.
5. 데이터베이스 어댑터는 방언별 동작을 구체화하며 JDBC/R2DBC 기반을 대체하지 않는다.
6. `exposed-workshop`, `exposed-r2dbc-workshop`, 실행 가능한 저장소 예제는 단계적으로 심화되는 실습을 제공한다.
7. 영속성 저장소에 책임을 과도하게 부여하지 않고 `bluetape4k-javers`가 감사/이력 관심사를 소유한다.

태그에 근거한 `root-readme-overview-01`과 `root-readme-module-relationships-01` 소스 객체를 승인된 어두운 스타일의 `repository-overview`와 `module-map`으로 다시 그린다. 두 SVG/PNG 쌍을 매니페스트에 등록하고 표시된 모든 모듈이 `1.11.0`에 존재하는지 검증한다.

- [ ] **4단계: 정규화 매니페스트를 다시 생성하고 검증한다**

```bash
ruby scripts/manual/export_manifest.rb docs/manual/manifest.yaml docs/manual/generated/manifest.json
ruby scripts/manual/export_manifest_test.rb
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
git diff --check
```

예상 결과: 정규화 JSON에 개요 다이어그램 등록 두 건이 포함되고, 두 번째 내보내기에서도 바이트 단위로 동일하며, YAML/JSON 일치 오류가 없다. 이 단계에서 검증은 작업 6~9에 의도적으로 배정한 상세 장 문서만 보고할 수 있다.

- [ ] **5단계: 진입 경로를 커밋한다**

```bash
git add docs/manual/en/index.md docs/manual/ko/index.md docs/manual/en/getting-started.md docs/manual/ko/getting-started.md docs/manual/en/architecture docs/manual/ko/architecture docs/manual/en/guides/learning-path.md docs/manual/ko/guides/learning-path.md docs/manual/assets/overview docs/manual/manifest.yaml docs/manual/generated/manifest.json
git commit -m "Give Exposed users one clear path from setup to production" \
  -m "Constraint: Users choose the central dependencies BOM while the manual version records documentation provenance." \
  -m "Confidence: high" \
  -m "Scope-risk: narrow" \
  -m "Tested: bilingual fact and link review; git diff --check."
```

### 작업 6: 기반, JDBC, R2DBC 매뉴얼 장을 작성한다

**파일:**
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-{bom,core,dao,jdbc,jdbc-tests,r2dbc,r2dbc-tests}.md`
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-core/{entity-id-model,mapping-conventions}.md`
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-jdbc/{transaction-ownership,repository-patterns,operations-testing}.md`
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-r2dbc/{coroutine-transactions,repository-patterns,cancellation-and-testing}.md`
- 생성: `docs/manual/{en,ko}/guides/{jdbc-vs-r2dbc,transaction-boundaries}.md`
- 생성: `docs/manual/assets/persistence/{path-decision,transaction-ownership}.svg`
- 생성: `docs/manual/assets/persistence/{path-decision,transaction-ownership}.png`

어두운 테마로 다시 그린 다이어그램은 `root-readme-overview-01`, `root-readme-module-relationships-01`, `exposed-jdbc-diagram-01`, `exposed-r2dbc-diagram-01`의 릴리스 근거 사실을 보존해야 한다. 이후 develop에만 추가된 모듈을 1.11 맵에 복사하면 안 된다.

- [ ] **1단계: 각 랜딩 페이지에 완전한 필수 섹션 계약을 작성한다**

모든 랜딩 페이지에는 다음 명시적 앵커가 있어야 한다.

```markdown
## Problem {#problem}
## When to use it {#when-to-use}
## Coordinates {#coordinates}
## Core concepts {#concepts}
## Quick start {#quick-start}
## API by task {#api-by-task}
## Recommended patterns {#patterns}
## Integrations {#integrations}
## Configuration {#configuration}
## Failure modes {#failures}
## Operations {#operations}
## Testing {#testing}
## Workshops and learning path {#workshops}
## Limitations {#limitations}
## Sources {#sources}
```

- [ ] **2단계: JDBC/R2DBC 비교를 홍보가 아닌 운영 관점으로 작성한다**

비교에서는 드라이버 API, 스레드 블로킹, 코루틴 취소, 트랜잭션 컨텍스트 전파, 커넥션 소유권, Spring 트랜잭션 통합, Testcontainers 전략, 마이그레이션 비용을 다뤄야 한다. R2DBC가 자동으로 더 빠르다는 주장을 명시적으로 거부해야 한다.

- [ ] **3단계: 어두운 테마의 다이어그램 두 개를 생성하고 검증한다**

검은색에 가까운 캔버스, 채도가 높은 파란색/청록색/녹색/주황색 강조색, 소스 크기 기준 최소 16px 본문 텍스트, 사이트 크기 조정 후에도 보일 만큼 큰 화살촉, 카드 경계에 닿는 간선 끝점, 선/카드 또는 텍스트/카드 겹침 없음, 소스 SVG와 렌더링된 PNG의 일치를 적용한다.

두 자산 모두 `bluetape-diagram` 체크리스트와 원본 크기 시각 검토를 수행한다. `path-decision` 다이어그램은 하나의 애플리케이션 결정을 JDBC 또는 R2DBC로 연결해야 한다. `transaction-ownership`은 애플리케이션/프레임워크, 트랜잭션 컨텍스트, 저장소, 드라이버, 데이터베이스를 연결된 화살표로 보여줘야 한다.

- [ ] **4단계: 안정 릴리스를 기준으로 소스 링크를 검증한다**

```bash
ruby scripts/manual/validate_release_manuals.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5
```

예상 결과: 완성된 기반/JDBC/R2DBC 문서의 모든 링크가 릴리스 트리에 존재한다.

- [ ] **5단계: 주요 영속성 경로를 커밋한다**

```bash
git add docs/manual
git commit -m "Explain the persistence boundaries users must choose explicitly" \
  -m "Rejected: Present JDBC and R2DBC as interchangeable syntax | Their driver, transaction, and cancellation contracts differ." \
  -m "Confidence: high" \
  -m "Scope-risk: moderate" \
  -m "Tested: manual validators, release-tree link validation, diagram checklist and visual review."
```

### 작업 7: 캐시, 직렬화, 암호화, 측정값 유형 매뉴얼을 작성한다

**파일:**
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-{cache,jdbc-caffeine,jdbc-lettuce,jdbc-redisson,r2dbc-caffeine,r2dbc-lettuce,r2dbc-redisson,fastjson2,jackson2,jackson3,tink,measured}.md`
- 생성: `docs/manual/{en,ko}/guides/{cache-selection,serialization-and-encryption}.md`
- 생성: `docs/manual/assets/cache/cache-selection.svg`
- 생성: `docs/manual/assets/cache/cache-selection.png`

캐시 다이어그램을 다시 그릴 때는 `exposed-cache-diagram-01`의 릴리스 근거 사실을 보존해야 한다.

- [ ] **1단계: 캐시 소유권과 수명주기를 문서화한다**

Caffeine, Lettuce, Redisson에 대해 로컬/분산 동작, 키 직렬화, 무효화, TTL, 클라이언트 수명주기, 코루틴 경계, 부분 실패, 테스트 격리를 기록한다. cache-aside, read-through, write-through, 무효화를 구분하며 일반적인 `put`을 write-through라고 부르면 안 된다.

- [ ] **2단계: 코덱과 암호화 선택을 문서화한다**

Jackson 2, Jackson 3, Fastjson2, Tink, 측정값 유형을 호환성, 마이그레이션 경계, 저장 데이터 안정성, 키 관리, 스키마 진화 측면에서 비교한다. Jackson 3은 태그 `1.11.0`에 존재하는 API만으로 설명해야 한다.

- [ ] **3단계: 검증하고 커밋한다**

```bash
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
ruby scripts/manual/validate_release_manuals.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5
git diff --check
git add docs/manual
git commit -m "Make cache and stored-data choices explicit for Exposed users" \
  -m "Confidence: high" \
  -m "Scope-risk: moderate" \
  -m "Tested: authoring contract, release contract, diagram checklist and visual review."
```

### 작업 8: 데이터베이스 및 분석 어댑터 매뉴얼을 작성한다

**파일:**
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-{postgresql,mysql8,duckdb,clickhouse,trino,bigquery,starrocks,cockroachdb}.md`
- 생성: `docs/manual/{en,ko}/guides/{database-adapter-matrix,oltp-vs-olap}.md`
- 생성: `docs/manual/assets/database/adapter-map.svg`
- 생성: `docs/manual/assets/database/adapter-map.png`

- [ ] **1단계: 어댑터마다 릴리스에 근거한 랜딩 페이지 하나를 작성한다**

각 페이지는 실제 통합 경계, 지원하는 쿼리/DDL 동작, 드라이버 및 Testcontainers 요구 사항, 트랜잭션 주의 사항, 배치/페이징 동작, 지원하지 않는 연산을 식별해야 한다. Druid는 1.11 매트릭스에 나타나면 안 된다.

- [ ] **2단계: OLTP/OLAP 결정 가이드를 작성한다**

가이드는 PostgreSQL/MySQL/CockroachDB의 트랜잭션 중심 사용과 ClickHouse/Trino/BigQuery/StarRocks/DuckDB의 분석 중심 사용을 비교해야 한다. 지연 시간, 쓰기 모델, 스키마 관리, 로컬 테스트, dry-run/예제 경로를 포함한다.

- [ ] **3단계: 검증하고 커밋한다**

```bash
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
ruby scripts/manual/validate_release_manuals.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5
git add docs/manual
git commit -m "Turn database adapters into an operational selection guide" \
  -m "Constraint: The stable matrix excludes the develop-only Druid adapter." \
  -m "Confidence: high" \
  -m "Scope-risk: moderate" \
  -m "Tested: authoring and release contracts; diagram checklist and visual review."
```

### 작업 9: 프레임워크, 배치, 솔버, 예제, 벤치마크 매뉴얼을 작성한다

**파일:**
- 생성: `docs/manual/{en,ko}/modules/bluetape4k-exposed-{batch,ktor,spring-boot-jdbc,spring-boot-r2dbc,spring-boot-batch,spring-modulith,timefold-solver-persistence}.md`
- 생성: `docs/manual/{en,ko}/modules/{examples-exposed-bigquery-dry-run,examples-exposed-clickhouse-oltp-olap,examples-ktor-exposed-demo,exposed-spring-boot-jdbc-demo,exposed-spring-boot-r2dbc-demo,benchmark-exposed-benchmark}.md`
- 생성: `docs/manual/{en,ko}/guides/{spring-and-ktor,testing-and-operations,hibernate-vs-exposed,audit-with-javers}.md`
- 생성: `docs/manual/assets/spring/{jdbc-auto-configuration,r2dbc-auto-configuration,modulith-publication}.{svg,png}`
- 생성: `docs/manual/assets/batch/runtime.{svg,png}`

- [ ] **1단계: 프레임워크 소유권을 설명한다**

Spring JDBC/R2DBC 페이지는 자동 구성, 트랜잭션 관리자, 빈 수명주기, 상태 점검/테스트 진입점, 블로킹/논블로킹 경계를 식별해야 한다. Ktor는 플러그인/애플리케이션 수명주기와 코루틴 컨텍스트를 식별해야 한다. Spring Modulith는 태그 `1.11.0`만 설명해야 하며 develop 전용 DDD 데모는 제외한다.

다이어그램은 태그에 근거한 소스 객체 `spring-boot-exposed-jdbc-diagram-01`, `spring-boot-exposed-r2dbc-diagram-01`, `spring-boot-exposed-spring-modulith-diagram-01`, `utils-batch-diagram-01`을 승인된 어두운 스타일로 다시 그려야 한다.

- [ ] **2단계: 예제를 실행 가능한 학습 경로로 구성한다**

각 예제 페이지는 사전 요구 사항, 명령, 예상 관찰 결과, 실패 진단, 다음 매뉴얼/워크숍 페이지를 명시해야 한다. 저장소의 실제 Gradle 프로젝트 경로와 릴리스 트리의 README/테스트 증거를 사용한다.

- [ ] **3단계: 저장소 간 심화 경로와 비교를 추가한다**

다음 링크를 명시적으로 추가한다.

```text
https://github.com/bluetape4k/exposed-workshop
https://github.com/bluetape4k/exposed-r2dbc-workshop
https://github.com/bluetape4k/bluetape4k-workshop
https://github.com/bluetape4k/bluetape4k-javers
https://www.jetbrains.com/help/exposed/home.html
```

`hibernate-vs-exposed.md`는 보편적인 승자를 선언하지 않고 추상화 수준, 엔티티 모델, 쿼리 모델, 트랜잭션 방식, fetch/lazy-loading 동작, 스키마 관리, 마이그레이션 비용을 비교해야 한다. `audit-with-javers.md`는 감사/이력/diff 책임을 영속성 저장소가 아닌 JaVers에 부여해야 한다.

- [ ] **4단계: 검증하고 커밋한다**

```bash
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
ruby scripts/manual/validate_release_manuals.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5
git diff --check
git add docs/manual
git commit -m "Connect Exposed modules to runnable production learning paths" \
  -m "Constraint: Cross-repository links supplement the manual but do not replace source-owned explanations." \
  -m "Confidence: high" \
  -m "Scope-risk: moderate" \
  -m "Tested: full manual and release validators; bilingual link review."
```

### 작업 10: 전체 소스 검증을 실행하고 소스 PR을 준비한다

**파일:**
- 수정: `docs/manual/generated/manifest.json`
- 수정: 기존의 저비용 작업에서 문서 검증을 아직 호출하지 않는 경우에만 `.github/workflows/ci.yml`
- 수정: `README.md`
- 수정: `README.ko.md`
- 수정: `AGENTS.md`
- 생성: `docs/superpowers/checklists/2026-07-14-exposed-1.11-manual-checklist.md`

- [ ] **1단계: 매니페스트를 다시 생성하고 깨끗하게 생성됨을 입증한다**

```bash
ruby scripts/manual/export_manifest.rb docs/manual/manifest.yaml docs/manual/generated/manifest.json
git diff --exit-code docs/manual/generated/manifest.json
```

예상 결과: 두 번째 생성 후 diff가 없다.

- [ ] **2단계: 전체 저비용 문서 게이트를 실행한다**

```bash
./gradlew exportManualModuleInventory
ruby -Itest scripts/manual/manual_contract_test.rb
ruby -Itest scripts/manual/release_contract_test.rb
ruby -Itest scripts/manual/export_manifest_test.rb
ruby -Itest scripts/manual/release_inventory_test.rb
ruby -Itest scripts/manual/generate_manuals_test.rb
ruby scripts/manual/release_inventory.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5 build/manual/module-inventory.json build/manual/module-inventory-1.11.0.json 40
ruby scripts/manual/validate_manuals.rb build/manual/module-inventory-1.11.0.json docs/manual/manifest.yaml
ruby scripts/manual/validate_release_manuals.rb 1.11.0 0b494a5fd1e083006046764757342b68a397e4c5
git diff --check
```

예상 결과: 모든 명령이 종료 코드 0으로 끝난다. 프로덕션 코드는 변경되지 않으므로 전체 Kotlin 테스트는 필요하지 않다. Gradle 구성/인벤토리 검증과 문서 계약만 실행한다.

- [ ] **3단계: 이중 언어 및 시각 검토를 수행한다**

모듈 ID 40개 모두에 영어와 한국어 랜딩이 있고, 등록된 모든 장의 로케일이 일치하며, 한국어 문장이 문장별 직역이 아니라 자연스러운지 확인한다. 모든 다이어그램이 `bluetape-diagram` 체크리스트와 원본 크기 검토를 통과하고, 잘린 다이어그램 텍스트나 화살표가 없는지도 확인한다.

정확한 합계도 단언한다. 로케일마다 Markdown 파일 62개(모듈 랜딩 페이지 40개 + 개요/가이드 페이지 14개 + 상세 장 8개), 전체 Markdown 파일 124개, SVG/PNG 쌍 10개에서 나온 다이어그램 파일 20개다.

- [ ] **4단계: 다시 생성한 아티팩트와 CI 연결 변경이 있으면 커밋한다**

```bash
git add docs/manual/generated/manifest.json README.md README.ko.md AGENTS.md docs/superpowers/checklists/2026-07-14-exposed-1.11-manual-checklist.md .github/workflows/ci.yml
git commit -m "Make the stable Exposed manual reproducibly publishable" \
  -m "Constraint: Documentation-only work uses focused validation instead of the expensive full code matrix." \
  -m "Confidence: high" \
  -m "Scope-risk: narrow" \
  -m "Tested: all manual tests, stable release validation, deterministic export, git diff --check."
```

`.github/workflows/ci.yml`을 변경할 필요가 없고 생성된 매니페스트가 이미 커밋되어 있다면 이 빈 커밋은 생략한다.

최종 문서 커밋 후 PR을 열기 전에 브랜치에 스테이징되지 않은 변경, 스테이징된 변경, 추적되지 않은 잔여 파일이 없음을 입증한다.

```bash
test -z "$(git status --porcelain)"
```

예상 결과: 출력 없이 종료 코드 0으로 끝난다.

- [ ] **5단계: 릴리스 메타데이터를 포함한 소스 PR을 생성한다**

`develop`을 대상으로 이슈/PR을 생성하고 둘 다 `debop`에게 할당하며 `1.11.0` 마일스톤과 복사한 레이블을 적용한다. PR 본문의 마지막 섹션이 정확히 `## DoD Status`인지 확인한다. 숫자 자리표시자를 남기지 말고 실제 번호를 저장한다.

```bash
ISSUE_URL="$(gh issue create --title 'Publish the release-faithful Exposed 1.11 manual' --body 'Create the bilingual source-of-truth manual for all 40 projects in release 1.11.0.' --assignee debop --milestone 1.11.0)"
ISSUE_NUMBER="${ISSUE_URL##*/}"
PR_URL="$(gh pr create --base develop --head docs/exposed-1.11-manual --title 'Publish the Exposed 1.11 source manual' --assignee debop --milestone 1.11.0 --body "Closes #${ISSUE_NUMBER}

## Summary

Publishes the bilingual, release-faithful Exposed 1.11 manual and its executable validation contract.

## DoD Status

- [x] 40 stable projects covered in English and Korean
- [x] release provenance and links validated
- [x] diagrams passed checklist and full-size review")"
PR_NUMBER="${PR_URL##*/}"
gh issue view "$ISSUE_NUMBER" --json labels --jq '.labels[].name' | while IFS= read -r label; do gh pr edit "$PR_NUMBER" --add-label "$label"; done
gh issue view "$ISSUE_NUMBER" --json assignees,labels,milestone,state
gh pr view "$PR_NUMBER" --json assignees,labels,milestone,body,headRefName,baseRefName
gh pr checks "$PR_NUMBER" --watch
```

실제 CI가 성공할 때까지 병합하지 않는다. 병합 후 병합된 소스 커밋을 기록한다. 해당 커밋이 웹사이트 스냅숏의 정확한 입력이 된다.
